import { SdkSpecView } from "./spec-view";

# SDK reference

Every export of `@secondlayer/sdk`, extracted from its TypeScript source.

<Callout type="note" title="Start with the guide">

This is the index. [SDK](/docs/sdk) explains the client, consumers, proofs, and signature verification in the order you'd actually use them.

</Callout>

<SdkSpecView />

export const metadata = {
	title: "SDK reference — Secondlayer Docs",
	description:
		"Every export of @secondlayer/sdk with its kind, signature, and source — extracted from TypeScript with openpkg, so it can't drift from the shipped package.",
};
