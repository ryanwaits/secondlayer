import { ApiReference } from "./api-reference";

# API reference

Every endpoint, parameter, and field, generated from the API's own OpenAPI description, with a link you can share for each one.

<Callout type="note" title="Start here">

The [SDK](/docs/sdk) already walks cursors, checkpoints, and rewinds reorgs. Credentials: [Authentication](/docs/authentication). Envelope, cursors, and filter grammar: [REST API](/docs/rest-api). Each endpoint's markdown is at `/docs/api-reference/<anchor>.md`, e.g. [`list-pox5-events.md`](/docs/api-reference/list-pox5-events.md).

</Callout>

<ApiReference />

export const metadata = {
	title: "API reference — Secondlayer Docs",
	description:
		"Every Secondlayer endpoint with parameters, objects, and example requests and responses, generated from the API's OpenAPI description.",
};
