import { SpecView } from "./spec-view";

# API reference

Every public endpoint, generated from the API's own OpenAPI description.

<Callout type="note" title="The SDK is the shorter path">

Cursor walking, checkpointing, and reorg rewind are already solved in [`@secondlayer/sdk`](/docs/sdk); reach for HTTP when you're not writing TypeScript. Envelope, cursor, and filter grammar live in [REST API](/docs/rest-api).

</Callout>

<SpecView />

export const metadata = {
	title: "API reference — Secondlayer Docs",
	description:
		"Every public Secondlayer endpoint with its parameters and response codes, generated from the API's OpenAPI description.",
};
