"use client";

export function HomeAnnotations() {
	return (
		<div className="prose">
			<p>Developer tools for Stacks.</p>
			<p>
				Atomic primitives that work the same way everywhere — as a CLI command,
				an API call, or a tool an agent picks up in a loop. Compose them to
				build on Stacks. New features are just new prompts.
			</p>
			<p>
				Open source. Self-host or use hosted. Built on{" "}
				<code>@secondlayer/stacks</code>, a viem-style SDK we dogfood across
				everything we ship.
			</p>
		</div>
	);
}
