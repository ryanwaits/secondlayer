"use client";

export function HomeAnnotations() {
	return (
		<div className="prose">
			<p>Agent-native developer tools for Stacks.</p>
			<p>
				Subgraphs turn onchain events into custom views of the chain. Subscribe
				to any row change with signed webhooks — run your own code anywhere.
			</p>
			<p>
				Use through the CLI, SDK, or MCP server — same auth, same operations,
				same patterns whether you&apos;re scripting a deploy or an agent is
				calling a tool in a loop. Open source. Self-host or use hosted.
			</p>
			<p>
				<code>@secondlayer/stacks</code> is the viem-style Stacks SDK we build
				everything on top of — general-purpose, open source, available to anyone
				building on Stacks.
			</p>
		</div>
	);
}
