# tools

API surface of `@secondlayer/stacks/tools`.

**Deprecated.** AI SDK `tool()` set for Stacks reads. Prefer [`@secondlayer/mcp`](https://www.npmjs.com/package/@secondlayer/mcp).

## Exports

`createStacksTools`, `getStxBalance`, `getAccountInfo`, `getBlock`, `getBlockHeight`, `readContract`, `estimateFee`, `bnsResolve`, `bnsReverse`, `getTransaction`, `getAccountHistory`, `getMempoolStats`, `getNftHoldings`.

`ai` and `zod` are optional peer dependencies.
