# tools/btc

API surface of `@secondlayer/stacks/tools/btc`.

**Deprecated.** AI SDK `tool()` set for Bitcoin reads via mempool.space (`BTC_MEMPOOL_URL` overrides the host). Prefer [`@secondlayer/mcp`](https://www.npmjs.com/package/@secondlayer/mcp).

## Exports

`btcConfirmations`, `btcBalance`, `btcUtxos`, `btcFeeEstimate`, `btcBlockHeight`, `BTC_ADDRESS`.
