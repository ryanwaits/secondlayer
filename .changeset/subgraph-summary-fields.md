---
"@secondlayer/shared": minor
---

Add optional fields to `SubgraphSummary`: `totalRows`, `lastError`, `lastErrorAt`, `updatedAt`, and `subscriptionCount` (powers the console subgraph cards' freshness, error surfacing, and subscription counts). All additive and optional, non-breaking.
