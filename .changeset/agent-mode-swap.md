---
"@secondlayer/web": patch
---

Docs agent mode replaces the page instead of stacking the prompt deck above a duplicate of the human article; the deck now links the page's raw-markdown endpoint for agents that need the reference.
