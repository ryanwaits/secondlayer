---
"@secondlayer/cli": minor
---

Accept `--json` on `sl streams` read commands (tip, events, reorgs, canonical) for uniform machine-readable output across all read commands. Streams already emits JSON; the flag documents the contract so an agent can pass it consistently.
