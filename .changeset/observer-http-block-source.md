---
"@secondlayer/subgraphs": minor
---

Experimental `SUBGRAPH_SOURCE=observer-http` BlockSource: page the indexer's internal observer-events HTTP for raw `/new_block` bodies. Default remains Postgres. Opt-in only (`OBSERVER_HTTP_URL`). Not a public `/v1` envelope.
