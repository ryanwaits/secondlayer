---
"@secondlayer/cli": patch
---

Docs: fix the `subgraphs create` help examples (they still showed the removed `subgraphs new`) and update the package README to the canonical command surface (`subscriptions create`, `account get`/`update`/`billing`, `contracts generate`, `local up`/`down`, `local db`, `projects`, and `SL_API_KEY`).
