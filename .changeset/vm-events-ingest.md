---
"@secondlayer/shared": minor
"@secondlayer/indexer": minor
---

Persist opt-in node `vm_events` on a second ordinal (`vm_event_index`). Classic `events` / Streams 1.0 cursor unchanged. Inner calls store as `nested_contract_call`.
