---
"@secondlayer/shared": patch
"@secondlayer/sdk": patch
---

Add `source.decoder_head` to archive `status.json` (the running Index decoder, distinct from last signed publish). Reword the `lagging` detail to name the Wednesday/Sunday publish cadence as the dominant expected lag.
