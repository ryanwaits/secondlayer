---
"@secondlayer/mcp": minor
---

Add the `index_codegen` MCP tool — generate a typed Prisma/Kysely/Drizzle/JSON-Schema for the public Index domain tables so an agent can scaffold a typed BYO-database mirror without the CLI.
