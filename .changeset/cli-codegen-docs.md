---
"@secondlayer/cli": patch
---

Extend the README with a full `sl generate` section covering code generation against local `.clar` files, deployed contracts, and the plugin system (`clarinet`, `actions`, `react`, `testing`). Remove unused `secrets.ts` + `workflows.ts` command stubs that were never registered.
