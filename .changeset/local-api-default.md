---
"@secondlayer/sdk": minor
"@secondlayer/cli": minor
"@secondlayer/mcp": patch
---

Default the SDK, CLI, and MCP to the local one-box API (`http://127.0.0.1:3800`). Override with `SL_API_URL`. Archive key fetches stay on the ops URL.
