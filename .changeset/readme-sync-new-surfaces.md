---
"@secondlayer/sdk": patch
"@secondlayer/mcp": patch
"@secondlayer/cli": patch
---

Sync package READMEs with the newly added surfaces: SDK datasets/contracts root clients, MCP datasets/index/streams/contracts tools + `secondlayer://context` resource + account update/billing, and CLI `sl index` / `projects delete` / data-products read commands.
