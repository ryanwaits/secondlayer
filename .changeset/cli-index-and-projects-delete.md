---
"@secondlayer/cli": minor
---

Add `sl index` (ft-transfers, nft-transfers, events, contract-calls) so the decoded Index layer has a CLI read path matching `sl datasets` and `sl streams`. Add `sl projects delete <slug>` (alias `rm`) and register `update` as an alias of `sl subgraphs deploy` (deploy is create-or-update).
