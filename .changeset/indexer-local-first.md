---
"@secondlayer/indexer": minor
---

Local-first block sourcing: tip-follower, integrity auto-backfill, and bulk-backfill try local DB before Hiro remote. Parser stores tx_index, API decode fallback now opt-in via ENABLE_TX_DECODE_FALLBACK.
