---
"@secondlayer/sdk": minor
---

Export the `IndexEvent` union members (`IndexFtTransfer`, `IndexPrint`, …) and the `consumeIndexFeed` types (`IndexFeedItem`, `IndexFeedEnvelope`, `IndexFeedFetcher`) from the package root, so consumers can name a narrowed event variant or a feed item directly.
