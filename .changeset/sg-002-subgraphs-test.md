---
"@secondlayer/mcp": patch
"@secondlayer/cli": patch
"@secondlayer/subgraphs": patch
---

MCP `subgraphs_test` runs handlers against Index with the same fail-closed EMPTY_MAPPING gate as the CLI; shared run lives in `@secondlayer/subgraphs/testing`.
