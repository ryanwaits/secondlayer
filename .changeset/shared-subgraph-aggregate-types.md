---
"@secondlayer/shared": minor
---

Add `SubgraphAggregateParams` and `SubgraphAggregateResponse` types for the subgraph aggregate query API (count/countDistinct as numbers, sum/min/max as lossless strings).
