---
"@secondlayer/stacks": minor
---

Add `@secondlayer/stacks/simnet`: a `simnet()` transport so `getContract` and public/wallet actions run against Clarinet simnet. Optional peer of that entry only — the root SDK still does not load clarinet-sdk.
