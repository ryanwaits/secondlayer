---
"@secondlayer/subgraphs": minor
---

Export `TYPE_MAP` (from `@secondlayer/subgraphs/schema`) and `VALID_FILTER_TYPES` (from `@secondlayer/subgraphs/validate`) so consumers can single-source the column-type and filter-type vocab instead of hand-duplicating it.
