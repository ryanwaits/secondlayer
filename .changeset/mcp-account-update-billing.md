---
"@secondlayer/mcp": minor
---

Add `account_update` (PATCH profile: display_name, bio, slug) and `account_billing` (plan + subscription status) tools so the MCP account surface matches the CLI's `account get/update/billing`.
