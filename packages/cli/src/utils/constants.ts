/**
 * Shared constants for @secondlayer/cli
 */

/**
 * Stacks boot-contract address, used as the default sender for read-only
 * calls when the user hasn't configured one.
 */
export const DEFAULT_SENDER_ADDRESS = "SP000000000000000000002Q6VF78";
