server=1
txindex=1
rpcallowip=0.0.0.0/0
rpcbind=0.0.0.0
# Edit these to match BITCOIN_RPC_USER / BITCOIN_RPC_PASSWORD in your .env,
# and match the credentials in Config.toml.
rpcuser=stacks
rpcpassword=changeme_bitcoin_rpc_password
# Prune: 0 = archive (required for Stacks); set to 550+ only for pruned chains.
prune=0
dbcache=4096
